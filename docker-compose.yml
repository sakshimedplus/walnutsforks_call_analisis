version: '3.9'

services:
  vite-app:
    build: .
    container_name: vite-supabase-app
    ports:
      - "80:80"
    environment:
      - VITE_SUPABASE_URL=${VITE_SUPABASE_URL}
      - VITE_SUPABASE_ANON_KEY=${VITE_SUPABASE_ANON_KEY}

    restart: always
